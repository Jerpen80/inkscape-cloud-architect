## 1. Document Resize

- [x] 1.1 Add resize function that takes the SVG element, bounding box, and margin, then sets width/height/viewBox
- [x] 1.2 Call resize at the end of `effect()` in `aws-auto-diagram.py`, reading margin from config
- [x] 1.3 Handle empty bounding box case (skip resize when nothing is rendered)
